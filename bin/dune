(executables
 (names muhfl_main)
 (modules muhfl_main)
 (libraries muhfl)
 (flags (-cclib -lstdc++))
 (link_flags (-linkall)))
(install
  (package muhfl)
  (section bin)
  (files (muhfl_main.exe as muhfl)))

(env (dev (flags (:standard -warn-error -a+31))))
